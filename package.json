{
  "name": "skip-wait",
  "version": "1.4.28",
  "description": "Bypass countdown timers and skip waiting pages. Instant access on link shorteners and file hosts - no wait, one click. Works on any site with wait timers.",
  "type": "module",
  "scripts": {
    "dev": "VITE_DEV_WATCH=1 vite build --watch & sleep 2 && vite build --config vite.content.config.ts --watch",
    "build": "tsc -b && vite build && vite build --config vite.content.config.ts",
    "build:zip": "bash scripts/build-zip.sh",
    "preview": "vite preview",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.4",
    "@types/chrome": "^0.1.40",
    "@types/node": "^25.6.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "playwright": "^1.59.1",
    "tailwindcss": "^4.2.4",
    "terser": "^5.46.2",
    "typescript": "^6.0.3",
    "vite": "^8.0.10"
  }
}
